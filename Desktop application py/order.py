import check as ch
import main_window as main_win
from welcome import welcome
from login import log_in






if "__name__" == "__main__":
    # display a welcome message(welcome window)
    well_will.display() # with a button click to continue
    # log in or sign in

    # confirm if the admin details are intact(that is if sign in)
    # if not move to the window to get details
    file_dict = {}
    file_dict = ch.window.get_details()
    if file_dict..... 


    # if login in
    #move to the login in window
    login.log_in()

    # main window containing soidiers details....
    MainWin = main_win()
