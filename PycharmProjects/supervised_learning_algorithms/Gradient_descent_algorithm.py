# Generate cost function



class linear_hypothsis():
    def __init__(self, parameters):
        self.theta_0 = parameters[theta_0]
        self.theta_1 = parameters[theta_1]
        return

    def linear_hyp(x):
        self.theta_0, self.theta_1,hypothesis = 0, 0,0
        hypothesis = theta_0 + theta_1 * x



class minimize_linear_hypothesis(linear_hypothesis):
    def __init__(self, **parameters):
        self.x = parameters[x]
        self.y = parameters[y]
        return

    def subtract_x_from_y(x):
        y = self.y
        return x-y

    def sumation(x,y):
        temp_val, diff, summation = 0,0,0
        temp_val = x.apply(linear_hyp)
        diff =

